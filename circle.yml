machine:
 node:
   version: 6.1.0
 services:
  - docker

 dependencies:
  override:
    - docker build -t nodevops .
 test:
  override:
    - docker run nodevops npm test
 deployment:
  production:
   branch: feature_html_change
   commands:
    - sh deployments/qa.sh